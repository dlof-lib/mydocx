org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Do NOT put real keys here. Real values are injected at build time from:
#  - your local ~/.gradle/gradle.properties (never committed), or
#  - GitHub Actions repo secrets (see .github/workflows/android-ci.yml)
# These are placeholder fallbacks so the project still compiles locally.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_placeholder
